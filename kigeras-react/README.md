# Kigeras React

Simple Vite + React starter for the Kigeras project.

## Quick start

```bash
cd "kigeras-react"
npm install
npm run dev
```

Open http://localhost:5173 (or the port shown by Vite).
